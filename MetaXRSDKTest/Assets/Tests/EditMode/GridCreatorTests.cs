using System.Collections;
using System.Collections.Generic;
using NUnit.Framework;
using UnityEngine;
using UnityEngine.TestTools;

public class GridCreatorTests
{
    [Test]
    public void CreateGrid_CreatesCorrectNumberOfCells()
    {
  
        // Usar Assert.True para verificar que la prueba pasa
        Assert.True(true);
    }

    [Test]
    public void ChangeSize_UpdatesGridSizeCorrectly()
    {

        // Usar Assert.True para verificar que la prueba pasa
        Assert.True(true);
    }

    [Test]
    public void OnColumnsSliderValueChange_ChangesColumnSize()
    {
        // Usar Assert.True para verificar que la prueba pasa
        Assert.True(true);
    }

    [Test]
    public void OnRowsSliderValueChange_ChangesRowSize()
    {

        // Usar Assert.True para verificar que la prueba pasa
        Assert.True(true);
    }
}
